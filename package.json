{
  "name": "create-99l-ground-station",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "99L rocket ground station for Windows and Linux",
  "main": "electron/main.mjs",
  "scripts": {
    "dev": "concurrently -k \"vite --config vite.config.js\" \"wait-on http://127.0.0.1:5173 && cross-env VITE_DEV_SERVER_URL=http://127.0.0.1:5173 electron .\"",
    "dev:renderer": "vite --config vite.config.js",
    "build:renderer": "vite build --config vite.config.js",
    "start": "electron .",
    "build": "npm run build:renderer && node scripts/package-desktop.mjs --publish never",
    "build:win": "npm run build:renderer && node scripts/package-desktop.mjs --win portable --x64 --publish never",
    "build:linux": "npm run build:renderer && node scripts/package-desktop.mjs --linux AppImage --x64 --publish never && npm run verify:linux-package",
    "verify:linux-package": "node scripts/verify-linux-package.mjs",
    "cli": "node scripts/ground-cli.mjs",
    "test": "node tests/run.mjs",
    "test:pty": "node tests/pty-integration.mjs"
  },
  "dependencies": {
    "serialport": "^13.0.0",
    "three": "^0.179.1"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "cross-env": "^10.0.0",
    "electron": "^43.2.0",
    "electron-builder": "^26.0.12",
    "vite": "^7.3.6",
    "wait-on": "^8.0.4"
  },
  "build": {
    "appId": "jp.create.rocket.groundstation99l",
    "productName": "CREATE 99L Ground Station",
    "asar": true,
    "npmRebuild": false,
    "directories": {
      "output": "release"
    },
    "files": [
      "dist/renderer/**/*",
      "electron/**/*",
      "shared/**/*",
      "node_modules/@serialport/**/*",
      "node_modules/debug/**/*",
      "node_modules/ms/**/*",
      "node_modules/node-addon-api/**/*",
      "node_modules/node-gyp-build/**/*",
      "node_modules/serialport/**/*",
      "package.json"
    ],
    "asarUnpack": [
      "node_modules/@serialport/bindings-cpp/**/*"
    ],
    "win": {
      "target": [
        "portable"
      ],
      "artifactName": "CREATE-99L-Ground-Station-${version}-Windows-x64.${ext}"
    },
    "linux": {
      "target": [
        "AppImage"
      ],
      "category": "Science",
      "artifactName": "CREATE-99L-Ground-Station-${version}-Linux-x64.${ext}"
    },
    "appImage": {
      "artifactName": "CREATE-99L-Ground-Station-${version}-Linux-x64.${ext}"
    }
  }
}
